import { PagePlaceholder } from '@/components/routing/PageHelpers';

export default function NotFoundPage() {
  return (
    <PagePlaceholder
      title="Page not found"
      description="That route doesn’t exist. Use the navigation to continue shopping."
    />
  );
}
